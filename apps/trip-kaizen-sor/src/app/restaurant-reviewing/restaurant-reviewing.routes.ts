import { Routes } from "@angular/router";
import { RestaurantReviewingComponent } from "./restaurant-reviewing.component";

export const RESTAURANT_REVIEWING_ROUTES: Routes = [
    { 
      path: '',
      component: RestaurantReviewingComponent,
    }
  ];